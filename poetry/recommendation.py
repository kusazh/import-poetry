# Recommendation

NewPackaging(OldPackaging(product))
month = 12

for each in old_story:
    for each in new_story:
        me.listen(each)

if promotion:
    affair += 1

refund.enable = False
review[me.name] = 'recommend'
refund.enable = False
