# The End of June

lies, promises = False, False
if light in here:
    here.remove(shadow)
else:
    here.append(shadow)
you.view.remove('red wall')

for each in news + gossip:
    sun.intensity += 1
    hurt += 1

if alley in mid(politics, love):
    window.is_open = True
    window.cover = 'flax'

if sunset:
    skirt = Skirt(night)
    skirt.weave()
    skirt.send(you)
